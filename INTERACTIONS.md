# pagewise · 交互清单

> SPEC.md 说"这个产品是什么、每个流程怎么走"；这一份补齐**每一个控件点下去发生什么、每个界面有哪些状态**，做到没有死路。
> 对照原型看最快：https://belendali.github.io/pagewise-mvp-design/ （右下角「原型」按钮可以直接跳到任意状态）

图例：**→** 表示跳转/结果，`⌘` 表示键盘操作。

---

## 0. 全局

| 控件 | 位置 | 行为 |
|---|---|---|
| 语言切换 EN/中文 | 顶栏（落地页、检查流程）/ 原型菜单 | 立即切换整站文案，不刷新、不丢状态。默认英文 |
| 登录 | 落地页顶栏 | → 打开登录弹窗（与注册同一个弹窗，标题不同） |
| Toast | 屏幕底部 | 3 秒自动消失，不阻塞操作。用于：已复制 / 已重命名 / 又回到你的列表 / 复查完成 / 原型提示 |
| Esc | 任意弹窗、下拉、导览 | 关闭最上层的浮层 |
| 点击浮层外部 | 下拉、菜单 | 关闭 |

---

## 1. 落地页

| 控件 | 行为 |
|---|---|
| 网址输入框 | 占位符轮播示例网址；聚焦后停止 |
| 免费检查 / ⌘Enter | 网址为空 → 聚焦输入框，不跳转；有值 → 进入第 1 步（读取网站） |
| 示例网址 chip | 填入输入框并立即开始检查 |
| 怎么用 · 四步 | 点任意一步 → 右侧演示切到该步，当前步高亮、其余淡化；右下角四个圆点指示进度 |
| 演示里的按钮（采用这段 / 放好了告诉我） | 往下一步走，不触发真实检查 |
| 顶栏「怎么用」 | 平滑滚动到该区块 |

**状态**：默认 / 输入中 / 提交后立即跳转（无 loading 态，下一屏自己有进度）

---

## 2. 三步检查

### 通用
- 顶部步骤条：已完成的步骤打勾且可点击回看；未到的步骤灰色不可点。
- 每一步都有「跳过」类的次要出口，不强制填写。
- 全流程不需要登录。

### 第 1 步 · 读取网站
| 元素 | 行为 |
|---|---|
| 三行进度（读取公开页面 / 理解业务与主要客户 / 准备要问的问题） | 依次从"处理中"变成打勾，右侧显示结果（如"已整理 3 个页面"） |
| 查看网站理解 → | 三行跑完后可点 → 第 2 步 |
| 不等了，先聊 | 直接 → 第 3 步 |

**失败态**：打不开这个网址 → 标题 + 三条常见原因（网址打错 / robots.txt 挡住 / 网站当时打不开）+「换一个网址」（回落地页并聚焦输入框）+「先看一个示例检查」。**这是唯一的失败出口，不能让用户卡住。**

### 第 2 步 · 确认理解
| 元素 | 行为 |
|---|---|
| 网站/品牌、网站介绍、主要市场·内容语言 | 只读展示；市场语言带「待确认」标签 |
| 有不准确的地方？修改 | 展开为可编辑表单 → 保存后回到只读态 |
| 理解正确，继续 | → 第 3 步 |
| 稍后完善，先聊 | → 第 3 步（不保存修改） |

### 第 3 步 · 选择问题
| 元素 | 行为 |
|---|---|
| 客户可能会问（展示 3 条，共 10 条） | 只读预览 |
| 和哪些品牌一起比较 | 展示自己 + 3 个竞品 |
| 调整问题或品牌 | 展开完整的 10 条列表 |
| 开始检查 | → 检查中 |
| 等下再说，先聊 | → 检查中（同样开始，只是不调整） |

### 检查中
- 进度条 + 「正在询问 ChatGPT · x/10」，逐条推进。
- 跑完 → 自动弹出注册。
- 期间离开页面（原型里不可能，真实产品要考虑）→ 结果保留，回来能继续。

---

## 3. 注册

| 元素 | 行为 |
|---|---|
| 使用 Google 继续 | → 直接进入工作区（原型里是假的） |
| 邮箱 + 用邮箱继续 | 邮箱格式错 → 输入框标红、提示；正确 → "去邮箱查收"态 |
| 打开链接（原型） | → 进入工作区 |
| 换一个邮箱 | 回到输入邮箱态 |
| 关闭 ✕ | 关闭弹窗，停留在结果页；结果页出现「创建工作区，查看结果」按钮（**不能让用户关掉弹窗后无路可走**） |

---

## 4. 工作区 · 顶栏

| 控件 | 行为 |
|---|---|
| 项目块 Answer.AI ▾ | 展开项目菜单 |
| 项目菜单 · 当前项目 | 带 ✓，点击收起菜单 |
| 换一个网站 | 就地展开输入框（预填当前网址）→ 开始检查 / ⌘Enter → 重新走三步检查 → 回工作区（**不再走新手引导**）；取消 / Esc 收起 |
| 添加另一个网站 | 「即将上线」标签 + toast |
| 项目设置 / 邀请同事 | 「即将上线」标签 + toast |
| 退出登录 | → 回落地页，清空登录态 |
| 头像 | toast（即将上线） |

---

## 5. 工作区 · 看板（左）

### 检查卡
| 控件 | 行为 |
|---|---|
| 立即检查 | 按钮变为「检查中 · x/10」，指标位置出现骨架屏；跑完 → 指标更新 + 差值标签、问题清单更新、右侧对话追加本次结果、toast |
| 自动检查开关 | 打开 → 展开频率选择（每天 / 每 3 天 / 每周，默认每 3 天）+ 「下次检查 X 日 · 随时可关」；关闭 → 收起 |
| 频率按钮 | 立即切换，文字里的下次时间跟着变 |

### 指标
- 两张卡：品牌被提及 / 本站被引用，`6 / 10` 形式。
- 第 2 次起显示与上次的差值（`较 9 月 16 日 +1`）。
- 检查进行中 → 骨架屏。
- 「查看指标明细」展开/收起补充信息。

### 所有问题
| 元素 | 行为 |
|---|---|
| 统计条 | `待办 N` / `已放到网站 N` / `已处理 x/y` + 进度条，随状态实时变 |
| 问题行 | 整行可点 = 点「查看」→ 右侧切到这条的对话，该行高亮（琥珀描边） |
| 行内容 | 状态圆点（已完成为绿）/ 标题 / `页面 · 第 N 次检查` / 状态标签 / 查看 |
| 翻页 | 超过 4 条出现 `‹ 1/2 ›`，首尾页对应按钮禁用 |
| 全部处理完 | 列表下方出现「这些都处理完了。」+ 一句下一步（自动检查开着 → "有变化我会告诉你"；关着 → "想看有没有变化，再查一次就行"） |
| 第 2 次检查后 | 列表上方出现一行琥珀色摘要：`第 2 次检查：闪卡页已被引用 · 定价页 1 条新建议 · 新增页面：首页` |

### 更多看板
- 排名追踪 / 网站体检 / 内容简报：点进去是该看板的独立页面（标题 + 一句说明 + 「这一版还没有」卡片）+ 「← 网站概览」返回。**不是点了没反应的装饰块。**

---

## 6. 工作区 · 对话（右）

### 顶部切换
| 控件 | 行为 |
|---|---|
| 当前对话 ▾ | 展开下拉：网站概览·总结 / 所有问题（按页面分组，每条带状态点和页面名）/ 对话（整站级） |
| 下拉里的条目 | 点击 → 切到该对话，下拉收起，左边对应行高亮 |
| 整站对话的 ✕ | 就地确认（删除 / 取消）→ 删除后回到网站概览 |
| ＋ | 新建一段整站对话 |

### 一条问题的对话（核心）
| 状态 | 内容 | 用户能做什么 |
|---|---|---|
| 待办 | 来源说明 + 为什么要改 + 建议卡（原文 / 新增） | 采用这段 / 先不改 / 追问 |
| 采用后 | 用户气泡「采用这段」+「已保存到草稿，网站还没有改动」+ 三步清单 | 复制（toast 已复制，序号打勾）/ 打开页面（新标签，原型是 toast）/ 已经放到网站上了 |
| 已放到网站 | 助手说明 AI 回答一般要几天才体现，建议开自动检查 | 每天 / 每 3 天 / 每周 三个按钮直接开启 |
| 先不改 | 用户气泡「先不改」+「这条我不会再提」 | **「还是改一下」→ 回到待办**（不能是死路） |
| 复测有结果 | 同一段对话里追加：ChatGPT 现在会引用它 ✓ + 前后对比条 | 继续追问 |

### 追问
| 控件 | 行为 |
|---|---|
| 预置追问 chip（每个页面 2 条） | 点击 = 直接发送该问题；发送过的 chip 消失 |
| 输入框 + 发送 / ⌘Enter | 发送后：用户气泡 → 打字动画 → 逐字回答 |
| 问同一页面的事 | 回答留在**当前这条对话**里 |
| 问整站的事 | 回答里带一个「在新对话里展开聊」按钮 → 新建整站对话并跳过去 |
| 问别的页面 | 指向那个页面的对话，给一个跳转按钮 |

---

## 7. 新手引导（只在第一次进工作区）

| 步 | 高亮 | 文案要点 | 控件 |
|---|---|---|---|
| 1/3 | 右上角项目块 | 你的网站在这里，目前一个项目 | 跳过 / 下一步 |
| 2/3 | 整个左半边看板 | 每次检查后，ChatGPT 提到你、引用你的情况 | 跳过 / 下一步 |
| 3/3 | **左边问题区 + 右边对话同时高亮** | 每一条在左边是一行，在右边是一段对话；点左边，右边就切过去 | 跳过 / 知道了 |

- 第 3 步若问题区不在视野内，先滚动到可见再高亮。
- 跳过 / 知道了 → 引导结束，之后不再出现（换网站、加网站都不会重来）。
- 想再看 → 原型菜单里有入口（真实产品可放在帮助里）。

---

## 8. 状态与空态一览

| 界面 | 空 | 加载 | 失败 | 完成 |
|---|---|---|---|---|
| 三步检查 | — | 逐项打勾 / x/10 进度 | **打不开这个网址**（三条原因 + 两个出口） | 弹注册 |
| 看板指标 | 首次检查中显示骨架 | 骨架屏 | （复查失败：保留上次数据 + 提示重试，待设计） | 数字 + 差值 |
| 问题清单 | 还没有优化项的说明文案 | — | — | 「这些都处理完了」+ 下一步 |
| 对话 | 网站概览的总结 + 一个入口按钮 | 打字动画 | — | — |
| 复查无变化 | — | 进度 | — | toast「和上次相比没有新变化」 |

---

## 9. 明确的交互原则

1. **任何一步都有出口。** 弹窗能关、失败有重试、"先不改"能反悔、下拉能收起。
2. **AI 不动用户的网站。** 采用 = 存草稿；上线 = 用户自己点确认。每一处都写明。
3. **用户的决定立刻反映到看板。** 在对话里做的事，左边那一行的状态同步变。
4. **不重复。** 同一件事不在两个地方各列一遍（所以助手开场白不再重列问题清单）。
5. **"即将上线"也要能点。** 点进去看到它将来是什么，而不是一个死标签。
6. **每次检查都有明确的"发生了什么变化"**，而不是只更新数字。

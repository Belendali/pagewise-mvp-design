# pagewise MVP · 产品规格说明

> 给接手实现的人（或 AI）看的文档。读完这一份 + 跑一遍原型，应该足够把这个 MVP 做出来。
>
> - 可点原型：https://belendali.github.io/pagewise-mvp-design/
> - 代码（单文件）：`index.html`
> - 交互清单（每个控件点下去发生什么、每个界面的空/加载/失败态）：`INTERACTIONS.md`
> - Figma（页面 **MVP**）：https://www.figma.com/design/7l0mqLoO0VOp1o4416Amgh?node-id=35-330 → **V3.0** 是流程板（20 张截图：onboarding / 闭环 / 细节 / 边界状态），**V3.1** 是原生设计稿（本地色彩·文字样式 + 组件 + 4 张工作区页面，可编辑、可标注）
> - 原型里的数据（Answer.AI 及其页面、问题、建议、指标）**全部是编的**，只用于演示。

---

## 1. 这是什么

**pagewise** 是给站长和小团队用的 GEO/SEO 工具：它替用户去问 ChatGPT 那些"客户会问的问题"，看用户的网站有没有被提到、被引用；没有的话，直接写好该加的文案，用户拍板后自己贴到网站上，然后再测一次，看有没有变化。

一句话的闭环：**发现问题 → 准备改动 → 用户拍板 → 执行复测。**

### 目标用户
- 有一个网站、没有 SEO 团队的店主 / 独立开发者 / 小团队市场
- 特征：看不懂专业 SEO 报告，能自己改网站文案，但不会写"该改什么"

### 这一版要验证什么
用户愿不愿意通过**「看板 + 对话」**这种形式来用一个 SEO/GEO 工具。投放少量广告，看落地页转化和首次检查完成率。

### 成功的样子（建议的观测口径）
1. 落地页 → 提交网址的转化率
2. 提交网址 → 走完三步检查的完成率
3. 检查完成 → 注册的转化率
4. 注册后 7 天内：至少采用 1 条建议的比例、标记"已放到网站"的比例、发起第 2 次检查的比例

---

## 2. 范围

### 这一版做
- 落地页（含四步说明的交互演示）
- 三步免费检查（不注册就能看到结果）
- 注册（邮箱 magic link / Google，原型里是假的）
- 工作区：看板 + 问题清单 + 每条问题一段对话
- 手动复查 + 可选的自动复查（每天 / 每 3 天 / 每周，默认关）
- 换网站（用户在落地页可能随手写了个网址）
- 中英文切换（默认英文）

### 这一版不做（界面上标"即将上线"，不要删掉入口，它们是留给用户的预期）
- 排名追踪（Rank tracking）
- 网站体检（Site audit）
- 内容简报（Content briefs）
- CMS 发布（用户自己复制粘贴）
- 多网站 / 多人协作（项目菜单里有入口，标"即将上线"）
- 邮件通知（自动检查之后理应有，但这一版不做）

### 明确的产品判断（别改掉）
1. **不接 CMS。** 我们只给文案和粘贴位置，网站的改动由用户自己完成。好处是无需接入即可使用，坏处是我们无法确认他到底贴没贴——所以有"已放到网站"这个用户自报的状态。
2. **检查默认手动触发。** 自动检查是副线开关，默认关闭，用户可随时关。
3. **一条问题 = 一段对话。** 不是一个页面一段，也不是一个大聊天。
4. **AI 从不直接改用户的网站。** 所有产出都是草稿，用户拍板。界面上反复强调这点（"保存到草稿 · 不发布"）。

---

## 3. 核心概念（数据模型）

```
Account
 └── Site (MVP: 每个账号一个)
      ├── domain               用户输入的网址
      ├── brand                品牌名（爬站得到，用户可改）
      ├── description          一句话业务描述（爬站得到，用户可改）
      ├── market / language    市场与内容语言（可待确认）
      ├── competitors[]        竞品品牌名
      ├── questions[]          要问 AI 的问题（10 条起）
      ├── autoCheck            { on: bool, freq: daily|3d|weekly }
      ├── Page[]               被检查过的页面：url / name
      ├── Check[]              每次检查
      │    ├── n               第几次
      │    ├── ranAt
      │    ├── metrics         { mentioned: 6, cited: 2, total: 10 }
      │    └── results[]       每个问题：是否提到品牌 / 是否引用本站 / 引用了谁
      └── Issue[]              每条"可以改的地方"
           ├── id
           ├── pageId          属于哪个页面
           ├── foundInCheck    第几次检查发现的
           ├── title           人话标题，如"补充 FAQ"
           ├── why             为什么要改（引用了哪个问题、竞品是谁）
           ├── evidence        AI 的原回答片段 + 被引用的竞品链接
           ├── currentText     页面上现在的原文
           ├── suggestion      { question, answer } 建议新增的内容
           ├── state           见下方状态机
           └── Chat            一段对话（消息数组）
```

### Issue 状态机（**规范名称**，界面文案见第 6 节）

```
todo ──open──▶ opened ──"采用这段"──▶ adopted ──"已放到网站"──▶ live ──复测被引用──▶ cited
  │                │
  └────────────────┴──"先不改"──▶ skipped
```

| 状态 | 含义 | 谁触发 |
|---|---|---|
| `todo` | 刚被检查发现，用户还没打开 | 系统 |
| `opened` | 用户打开过它的对话 | 用户 |
| `adopted` | 用户点了"采用这段"，内容存为草稿 | 用户 |
| `live` | 用户点了"已放到网站上了" | 用户自报 |
| `cited` | 下一次检查发现这一页真的被 AI 引用了 | 系统 |
| `skipped` | 用户点了"先不改"，之后的检查不再重复提 | 用户 |

- `skipped` 的问题在后续检查里**不再出现**，但仍留在清单里（灰色），用户可以随时重新打开。
- 同一个页面可以有多条 issue；第 2 次检查在同一页发现的新问题，**新建一条 issue + 新开一段对话**，不要追加到旧对话里。

---

## 4. 端到端流程

### 4.1 落地页
- Hero：一句问句标题（"ChatGPT 会推荐你吗？"）+ 一个网址输入框 + "免费检查"
- 下方「怎么用」四步，右侧是一个**可点的演示**：1 找到缺口 → 2 拿到改法 → 3 你来决定 → 4 再查一次。演示里用的组件必须和真实产品一致（同一套对话气泡、建议卡、复制粘贴清单、前后对比条）。
- 不需要登录即可提交网址。

### 4.2 三步免费检查（不注册）
| 步骤 | 屏幕 | 系统做的事 | 用户做的事 |
|---|---|---|---|
| 1 | 读取网站 | 抓取公开页面，识别品牌、业务、主要页面 | 等待（约 10-20 秒），看到逐项打勾 |
| 2 | 确认理解 | 展示品牌 / 一句话介绍 / 市场·语言 | 确认，或改掉不准的地方 |
| 3 | 选择问题 | 给出 10 个"客户可能会问"的问题 + 竞品列表 | 直接开始，或调整问题/竞品 |
| — | 检查中 | 逐条去问 AI，显示 x/10 进度 | 等待 |
| — | 结果 | 出结果后立即要求注册 | 注册（邮箱或 Google） |

**设计要点**
- 三步是线性的，不是对话。用户第一次来还不知道我们是干什么的，对话会让他不知道该说什么。
- 每一步都能看到"已经完成了什么"，不要空白等待。
- 注册卡在结果之前**不合适**——先让他看到价值，再要邮箱。当前实现是：检查跑完 → 弹注册 → 注册后进入工作区。

### 4.3 工作区（B3 布局）

```
┌──────────────────────────────────┬──────────────────┐
│ 顶栏：logo · 项目 ▾ · 账号        │ 对话切换 ▾   ＋  │
├──────────────────────────────────┤                  │
│ 检查（上次时间 + 立即检查 + 自动）│  当前这条问题的  │
│ 两个指标（被提及 / 被引用）       │  对话内容        │
│ ── 所有问题 ──                    │                  │
│ 待查看 4 · 已放到网站 1 · 进度条  │                  │
│ [ 问题行 ] [ 问题行 ] …  ‹ 1/2 ›  │                  │
│ 更多看板（即将上线 ×3）           │  追问 chips      │
│                                   │  输入框 + 发送   │
└──────────────────────────────────┴──────────────────┘
        左：约 66%                        右：clamp(340, 29vw, 450)
```

**左（看板）＝ 现在怎么样、还剩什么。右（对话）＝ 这一条具体怎么改。**

- 点左边任意一条问题 → 右边切到它的对话，该行高亮。
- 右边顶部的下拉可以切换对话（网站概览 · 总结 / 所有问题 / 整站对话），**用下拉不用 tab**，因为问题会越来越多。
- 每条对话里可以继续追问，跟用大模型聊天一样。对话内容只属于这一条问题。

#### 一条问题的对话长什么样
1. 一句来源说明：`第 2 次检查在定价页（answerai.pro/pricing）发现的。这一条改动的所有事情都留在这段对话里。`
2. 为什么要改：`大家拿你和 Chegg 比较时，ChatGPT 没有引用你的定价页。我写好了一段可以加上去的内容，你决定用不用：`
3. 建议卡片：`网站原文`（灰） → `新增`（琥珀色块，问句 + 答案）
4. 决定：`采用这段` / `先不改`，下面小字 `保存到草稿 · 不发布`
5. 采用后：`已保存到草稿，网站还没有改动。` + 三步清单
   1. 复制这段文字 → `复制`
   2. 粘贴到定价页 → `打开页面 ↗`
   3. 放上去后告诉我 → `已经放到网站上了`
6. 标记已上线后：助手提醒 AI 的回答一般要几天才体现，建议开自动检查（给每天/每 3 天/每周三个按钮）
7. 下一次检查后：如果这一页被引用了，在**同一段对话**里追加结果 + 前后对比（9 月 16 日 没提到 → 9 月 25 日 提到·并引用）

#### 复查
- `立即检查`：逐条重问，进度可见；跑完后
  - 指标更新并显示与上次的差值（`+1 较 9 月 16 日`）
  - 已上线的 issue 如果被引用 → 状态变 `cited`
  - 新发现的问题 → 新 issue + 新对话，清单里带"第 2 次检查"标注
  - 新发现的页面 → 新页面 + 它的 issue
  - `skipped` 的不再提
- `自动检查`：开关 + 每天/每 3 天/每周，显示下次时间，随时可关。

#### 换网站
项目 ▾ → `换一个网站` → 就地输入新网址 → 重新走三步检查 → 回到工作区（**不再走新手引导**）。
说明文案：`会对这个网址重新走一遍三步检查。`

#### 新手引导（只在第一次进工作区）
三步 coach mark：
1. 你的项目 —— 高亮右上角项目块
2. 看板 —— 高亮整个左半边
3. 任务和对话是一一对应的 —— **同时高亮左边的问题区和右边的对话**，文案：`我们发现的每一条，在左边是一行，在右边是一段对话。点左边任意一行，右边就切到它。`

---

## 5. 界面规格

### 布局
| 区域 | 宽度 | 背景 |
|---|---|---|
| 看板（左） | `minmax(0,1fr)` | 在底色上叠 4% 白，能透出粒子底纹 |
| 对话（右） | `clamp(380px, 34vw, 520px)` | `#0C0B0A` 实色，左边一条 `#26251F` 分隔线 |

- 页面底色 `#0F0F0F`；滚动条深灰 `#2E2E2B`，轨道透明。
- 左边亮、右边暗，用明暗区分"数据层"和"工作层"。
- 三栏（看板 / 问题清单 / 对话）的变体也做过（原型里叫 B4），最终选了两栏，问题清单放在看板里。

### 视觉 token
```
底色      #0F0F0F      面板 #131313 / #171717      分隔线 #262625 / #34332F
正文      #F1EEE8      次要 #A9A59D                 更次要 #6F6B64
强调色    #E0B36A（琥珀）   新增块底 rgba(224,179,106,.07) 边 rgba(224,179,106,.35)
成功/已上线 #9DB8A0
主按钮    #DAD7D0 底 + #111 字 + 内阴影做成凸起的复古按钮
```
字体：标题 Newsreader（中文 Noto Sans SC），正文 Geist，数字和标签 Geist Mono。

### 组件
- **指标卡**：标题 + 大数字 `6 / 10` + 变化标签
- **问题行**：状态圆点 / 标题 / `页面 · 第 N 次检查` / 状态标签 / `查看`
- **建议卡**：卡头 `页面 · 问题标题`；`网站原文`（灰）；`新增`（琥珀块）；卡脚按钮
- **三步粘贴清单**：序号 + 说明 + 右侧按钮
- **前后对比条**：`日期 / 旧状态` → `日期 / 新状态`
- **对话切换下拉**：当前项 + ▾，展开后分组（网站概览 / 所有问题 / 对话），整站对话可删

---

## 6. 文案

### 原则
1. **说人话**，不用 SEO 黑话（不要说 "schema"、"canonical"、"E-E-A-T"）。
2. **每一句都告诉用户接下来能做什么**。
3. **反复强调不会动他的网站**。
4. 英文为默认，中文是翻译；中文不要翻译腔。

### 状态词（界面显示）
| 规范状态 | 中文 | English |
|---|---|---|
| todo / opened | 待办 | To do |
| adopted（未粘贴） | 草稿已保存 · 待粘贴 | Draft saved · paste it |
| live | 已放到网站 | On your site |
| cited | 已被引用 ✓ | Now cited ✓ |
| skipped | 先不改 | Skipped |

> `todo` 和 `opened` 对用户是同一件事（还没决定），界面上合并显示为「待办 / To do」；"是否打开过"用未读圆点表达，不单独占一个状态词。

### 关键文案（中 / EN）
- 落地页标题：`ChatGPT 会推荐你吗？` / `Does ChatGPT recommend your business?`
- 检查卡：`你发起时才检查 · 第 1 次检查 · 上次 9 月 16 日 · 09:00` / `Runs only when you ask · check #1 · last Sep 16 · 09:00`
- 指标：`品牌被提及` `本站被引用` / `Brand mentioned` `Your site cited`
- 决定：`采用这段` `先不改` / `Use this` `Not now`
- 保存提示：`保存到草稿 · 不发布` / `Saves to draft · doesn't publish`
- 采用后：`已保存到草稿，网站还没有改动。` / `Saved to draft. Your website hasn't changed.`

---

## 7. 后端要提供什么

按接口契约描述，具体实现方式不限。

### `POST /sites/analyze`  （三步的第 1、2 步）
入参：`{ domain }`
出参：`{ brand, description, market, language, pages: [{url, name}], competitors: [] }`
做法：抓取公开页面 → 用 LLM 总结品牌 / 业务 / 主要页面 / 可能的竞品。

### `POST /sites/{id}/questions:suggest` （第 3 步）
出参：`{ questions: [ {text, intent} ], competitors: [names] }`
要求：问题必须是**客户会真的去问 AI 的口语问题**（"波特兰哪里能订无麸质生日蛋糕"），不是关键词。

### `POST /sites/{id}/checks` （执行一次检查）
- 对每个问题，向目标模型提问（MVP：ChatGPT；后续 Perplexity / Google AI 等）
- 每条记录：`mentionedBrand`, `citedOurSite`, `citedDomains[]`, `answerExcerpt`
- 汇总 `metrics.mentioned / cited / total`
- 流式返回进度（前端显示 x/10）

### `POST /sites/{id}/issues:generate` （检查之后）
输入：这次检查的结果 + 页面原文
输出：若干 issue，每条包含 `pageId, title, why, evidence, currentText, suggestion{question, answer}`
要求：
- `suggestion` 是**可以直接粘贴的成品文案**，不是"建议你增加一段关于 X 的内容"
- 语气、语言要跟用户网站一致
- 一次检查产出 3-5 条即可，别一次给 20 条

### `POST /issues/{id}/messages` （对话追问）
上下文：该 issue 的所有信息 + 这一页的原文 + 最近的检查结果。
限制：回答里不能承诺"我已经帮你改好了"，只能给草稿。

### 定时任务
自动检查：按 `autoCheck.freq` 触发 `checks`，有变化时（理想情况）发邮件——邮件这一版不做。

---

## 8. 埋点（投放测试用）

| 事件 | 触发点 |
|---|---|
| `landing_view` | 落地页曝光 |
| `url_submitted` | 提交网址 |
| `step_confirm_done` / `step_scope_done` | 三步的第 2、3 步完成 |
| `check_completed` | 第一次检查跑完 |
| `signup_started` / `signup_completed` | 注册弹窗 / 注册成功 |
| `issue_opened` | 打开一条问题 |
| `suggestion_adopted` / `suggestion_skipped` | 采用 / 先不改 |
| `marked_live` | 标记已放到网站 |
| `recheck_started` | 手动复查 |
| `autocheck_enabled` | 打开自动检查（含频率） |
| `site_changed` | 换网站 |

---

## 9. 已知简化与待定

1. **"已放到网站"由用户自报**，我们无法验证。下一次检查会抓一次该页面，如果没找到新增内容，应该提示用户（这一版没做）。
2. **只支持一个网站**。项目菜单里"添加另一个网站"是占位。
3. **邮件通知没做**，自动检查跑完用户只能自己回来看。
4. **竞品是系统给的**，用户能改但改得不多，后续要考虑竞品管理。
5. **多语言**：界面中英文，检查的问题和建议目前默认英文市场。
6. 原型里的注册是假的（点"打开链接（原型）"直接进工作区）。
7. 原型不会真的抓取用户输入的网址，任何网址都会用示例网站 answerai.pro 的数据，并弹一条提示说明。

---

## 10. 原型代码对照

单文件 `index.html`（无构建、无依赖，直接用浏览器打开即可），约 2600 行，结构：

| 区块 | 内容 |
|---|---|
| `<style>` | 全部样式。工作区相关在 `WORKSPACE` 注释之后；`body.inner` 是 app 的暖色深色皮肤 |
| `T = {en, zh}` | 所有文案。加功能时两种语言都要加 |
| `S` | 全局状态（view / domain / user / ws 工作区状态） |
| `renderDiag()` | 三步检查的三个屏 |
| `renderWS()` | 工作区总入口，决定左中右渲染什么 |
| `wsOverview()` / `issueRows()` | 看板 + 问题清单 |
| `wsChat()` / `issueChat()` | 右侧对话 |
| `cvMenu()` / `wsTopbar()` | 对话切换下拉 / 顶栏与项目菜单 |
| `act(name, el)` | 所有点击行为的集中派发 |
| `renderPreview()` | 落地页「怎么用」四步演示 |

调试入口：右下角「原型」按钮可以直接跳到任意状态，也能切中英文。

---

## 11. 建议的实现顺序

1. 落地页 + 三步检查（前端）+ `analyze` / `questions:suggest`（后端）
2. `checks` 真正跑起来（先只接一个模型），指标算出来
3. 注册 + 工作区骨架（看板两个数字 + 问题清单）
4. `issues:generate` + 一条问题一段对话 + 采用/先不改/复制粘贴/已上线
5. 第二次检查的差值、状态流转、新问题新对话
6. 自动检查开关 + 定时任务
7. 换网站、项目菜单、埋点
8. 之后才是：邮件、排名追踪、网站体检、内容简报、CMS
